---
name: zenhub-eval-transfer
description: Run ZenHub-style evaluation transfer scripts from rag_eval (evals/zh-* and related) into Pratt-Backend. Use when porting ZH ticket automation, pytest stubs, or fixtures generated by zh-*-transfer.py scripts.
---

# ZenHub eval transfer scripts

## When to use

- Working with **`evals/zh-*-transfer.py`** (or phase-folder variants), **`evals/folder-keyword-search-transfer.py`**, or similar **ticket porting** automation.
- Following **`evals/ragas-implementation-runbook.md`** or ticket summary markdown in **`evals/`**.

## Rules

1. **Assume `cwd` = Pratt-Backend root** unless the specific script documents a different layout (nested `backend/`).

2. Prefer **`uv run python`** / **`uv run pytest`** when available; on **Windows** without `uv`, use **`py -3`** (`py -3 -m pytest`). Scripts may auto-select `uv` for pytest.

3. Generated tests are sometimes **gitignored** — scripts may use **`git add -f`** for test paths; match that pattern when committing manually.

4. Do not embed **secrets** in transfer scripts or generated files; use env and Django settings.

## Verification

- Run the script from backend root, then **`uv run pytest`** or **`py -3 -m pytest`** on the generated test path printed by the script.
- Confirm branch and file paths match the ticket’s expected module layout.
